<UserControl x:Class="WpfApp1.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WpfApp1.ViewModels"
             xmlns:conv="clr-namespace:WpfApp1.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1250">

    <UserControl.DataContext>
        <vm:InventoryViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

        <Style x:Key="CyberInput" TargetType="TextBox">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4000FFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="CaretBrush" Value="Cyan"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="Cyan"/>
                                <Setter TargetName="border" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="#10FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2500FFFF"/>
            <Setter Property="Padding" Value="15"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#08FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#0FFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2000FFFF"/>
                        <Setter Property="Foreground" Value="#00FFFF"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#00FFFF"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#020205" Offset="0.0"/>
                <GradientStop Color="#0A122A" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource GlassPanel}" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" Fill="#00FFFF" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>

                    <StackPanel Margin="5">
                        <TextBlock Text="BARCODE_ID" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold"/>
                        <TextBox Text="{Binding NewBarcode}" Width="120" Style="{StaticResource CyberInput}"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="PRODUCT_LABEL" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold"/>
                        <TextBox Text="{Binding NewProductName}" Width="220" Style="{StaticResource CyberInput}"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="COST_UNIT" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold"/>
                        <TextBox Text="{Binding NewCost}" Width="80" Style="{StaticResource CyberInput}"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="SELL_UNIT" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold"/>
                        <TextBox Text="{Binding NewSellingPrice}" Width="80" Style="{StaticResource CyberInput}" Foreground="#00FF88" BorderBrush="#00FF88"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="MIN_RESERVE" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold"/>
                        <TextBox Text="{Binding NewMinStock}" Width="80" Style="{StaticResource CyberInput}" Foreground="#FF4444" BorderBrush="#FF4444"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="TARGET_NODE" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold"/>
                        <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding TargetWarehouseForNewProduct}" DisplayMemberPath="Name" Width="160"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="15,0,0,5">
                        <Button Content="INITIALIZE" Command="{Binding AddProductCommand}" Width="100" Style="{StaticResource CyberButton}" Visibility="{Binding SelectedProductForEdit, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverted}"/>
                        <Button Content="UPDATE" Command="{Binding UpdateProductCommand}" Width="100" Style="{StaticResource CyberButton}" BorderBrush="Lime" Foreground="Lime" Visibility="{Binding SelectedProductForEdit, Converter={StaticResource NullToVisibilityConverter}}"/>
                        <Button Content="ABORT" Command="{Binding ClearFormCommand}" Width="80" Style="{StaticResource CyberButton}" BorderBrush="Gray" Foreground="Gray" Margin="10,0,0,0" Visibility="{Binding SelectedProductForEdit, Converter={StaticResource NullToVisibilityConverter}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#0AFFFFFF" CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="NETWORK_SCOPE:" Foreground="#00FFFF" VerticalAlignment="Center" Margin="0,0,10,0" FontFamily="Consolas"/>
                        <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name" Width="200" Opacity="0.8"/>
                        <Button Content="RESET" Command="{Binding ClearWarehouseFilterCommand}" Margin="10,0,0,0" Background="Transparent" Foreground="#88FFFFFF" BorderThickness="0"/>
                        <Separator Width="1" Margin="20,5" Background="#20FFFFFF"/>
                        <TextBlock Text="SEARCH_QUERY:" Foreground="#00FFFF" VerticalAlignment="Center" Margin="0,0,10,0" FontFamily="Consolas"/>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="300" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="#5000FFFF" Foreground="White" Padding="5" CaretBrush="Cyan"/>
                    </StackPanel>
                    <Button Content="📥 PDF_REPORT" Command="{Binding GeneratePDFReportCommand}" HorizontalAlignment="Right" Style="{StaticResource CyberButton}" Width="150" BorderBrush="#FFD700" Foreground="#FFD700"/>
                </Grid>
            </Border>

            <Border Grid.Row="2" Background="#10FFFFFF" CornerRadius="12" Padding="5">
                <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionChanged="DataGrid_SelectionChanged">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#10FFFFFF"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                    <Setter Property="Background" Value="#25FF0000"/>
                                    <Setter Property="Foreground" Value="#FF8888"/>
                                </DataTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1500FFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="BARCODE" Binding="{Binding Barcode}" Width="120"/>
                        <DataGridTextColumn Header="PRODUCT_NAME" Binding="{Binding Name}" Width="*"/>
                        <DataGridTemplateColumn Header="STOCK_LVL" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#20FFFFFF" CornerRadius="4" Padding="5,2">
                                        <TextBlock Text="{Binding StockLevel}" HorizontalAlignment="Center" FontWeight="Bold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="COST" Binding="{Binding CostPrice, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="PRICE" Binding="{Binding UnitPrice, StringFormat=C}" Width="100"/>

                        <DataGridTemplateColumn Header="OPERATIONS" Width="430">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Text="{Binding DataContext.TransactionQuantity, RelativeSource={RelativeSource AncestorType=DataGrid}, UpdateSourceTrigger=PropertyChanged}" 
                                                 Width="45" Background="#20000000" Foreground="White" BorderBrush="#5000FFFF" Margin="2" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                                        <Button Content="▲ IN" Command="{Binding DataContext.ProcessTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="In" Style="{StaticResource CyberButton}" Width="55" Margin="2" FontSize="10"/>
                                        <Button Content="▼ OUT" Command="{Binding DataContext.ProcessTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="Out" Style="{StaticResource CyberButton}" Width="60" Margin="2" FontSize="10" BorderBrush="Orange" Foreground="Orange"/>
                                        <Button Content="⇄ TRANSFER" Command="{Binding DataContext.OpenTransferDialogCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource CyberButton}" Width="90" Margin="2" FontSize="10" BorderBrush="Yellow" Foreground="Yellow"/>
                                        <Button Content="✎ EDIT" Command="{Binding DataContext.PrepareUpdateCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource CyberButton}" Width="77" Margin="2" FontSize="10"/>
                                        <Button Content="✖ DELETE" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource CyberButton}" Width="79" Margin="2" FontSize="10" BorderBrush="#FF003C" Foreground="#FF003C"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>