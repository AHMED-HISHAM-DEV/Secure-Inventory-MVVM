<UserControl x:Class="WpfApp1.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WpfApp1.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.DataContext>
        <vm:DashboardViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#00FFFF"/>
                    <Setter Property="Background" Value="#25FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NeonLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00FFFF" BlurRadius="10" ShadowDepth="0" Opacity="0.7"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#020205" Offset="0.0"/>
                <GradientStop Color="#0A0F1E" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="0,0,0,20" BorderBrush="#3300FFFF" BorderThickness="0,0,0,1" Padding="0,0,0,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Data="M12,2L4.5,20.29L5.21,21L12,18L18.79,21L19.5,20.29L12,2Z" 
                              Fill="#00FFFF" Width="24" Height="24" Stretch="Uniform" Margin="0,0,15,0"/>
                        <TextBlock Text="CORE_DASHBOARD_V2.1" Foreground="#00FFFF" FontSize="20" FontWeight="Bold" FontFamily="Consolas"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border Background="#1A00FFFF" Padding="10,5" CornerRadius="5" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Fill="Lime" Margin="0,0,8,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Style.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.2" Duration="0:0:0.8" AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="UPLINK_STABLE" Foreground="Lime" FontSize="10" VerticalAlignment="Center" FontFamily="Consolas"/>
                            </StackPanel>
                        </Border>

                        <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}"
                                  DisplayMemberPath="Name" Width="200" Background="#111" Foreground="Cyan" BorderBrush="#3300FFFF"/>
                        <Button Content="RESET" Command="{Binding ClearFilterCommand}" Style="{StaticResource CyberButton}" Width="70" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <UniformGrid Grid.Row="2" Columns="3" Margin="-10">
                <Border Style="{StaticResource GlassCard}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="TOTAL ASSET VALUE" Foreground="#88FFFFFF" FontSize="11" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding TotalInventoryValue, StringFormat={}{0:C}}" Foreground="White" FontSize="38" Style="{StaticResource NeonLabel}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GlassCard}" BorderBrush="#FF003C">
                    <Border.Background>
                        <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="1" RadiusY="1">
                            <GradientStop Color="#15FF003C" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Border.Background>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="SECURITY_BREACH: LOW STOCK" Foreground="#FF003C" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding LowStockCount}" Foreground="#FF003C" FontSize="60" FontWeight="Bold" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#FF003C" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GlassCard}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="ESTIMATED REVENUE" Foreground="#88FFFFFF" FontSize="11" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding PotentialProfit, StringFormat={}{0:C}}" Foreground="#FFD700" FontSize="38">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#FFD700" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Border Background="#CC050505" CornerRadius="10" BorderBrush="#3300FFFF" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="#22FFFFFF" Padding="15,8" CornerRadius="10,10,0,0">
                            <TextBlock Text="TERMINAL_v4.2 // ACTIVITY_STREAM" Foreground="#88FFFFFF" FontSize="10" FontFamily="Consolas"/>
                        </Border>
                        <ListBox Grid.Row="1" ItemsSource="{Binding TerminalLogs}" Background="Transparent" BorderThickness="0" Padding="15">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,3">
                                        <TextBlock Text="[root@cyberstock]:~#" Foreground="#00FFFF" FontFamily="Consolas" Opacity="0.6" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding}" Foreground="#A0FF00" FontFamily="Consolas" FontSize="13"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>