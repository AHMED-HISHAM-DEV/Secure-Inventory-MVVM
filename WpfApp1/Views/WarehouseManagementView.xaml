<UserControl x:Class="WpfApp1.Views.WarehouseManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WpfApp1.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">

    <UserControl.DataContext>
        <vm:WarehouseViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="CyberInput" TargetType="TextBox">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4000FFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="CaretBrush" Value="Cyan"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="Cyan"/>
                                <Setter TargetName="border" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CyberButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#00FFFF"/>
            <Setter Property="BorderBrush" Value="#00FFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#2000FFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#020205" Offset="0.0"/>
                <GradientStop Color="#0A122A" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#15FFFFFF" CornerRadius="12" BorderBrush="#2500FFFF" BorderThickness="1" Padding="20" Margin="0,0,0,25">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M12,3L2,12H5V20H19V12H22L12,3M12,7.7C14.1,7.7 15.8,9.4 15.8,11.5C15.8,14.5 12,18.8 12,18.8C12,18.8 8.2,14.5 8.2,11.5C8.2,9.4 9.9,7.7 12,7.7Z" 
                          Fill="#00FFFF" Width="24" Height="24" Margin="0,0,20,0" VerticalAlignment="Center"/>

                    <StackPanel Margin="5">
                        <TextBlock Text="NODE_IDENTIFIER" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold" Margin="2,0,0,3"/>
                        <TextBox Text="{Binding NewName}" Width="200" Style="{StaticResource CyberInput}"/>
                    </StackPanel>

                    <StackPanel Margin="15,5,5,5">
                        <TextBlock Text="GEOLOCATION_COORDINATES" Foreground="#8800FFFF" FontSize="9" FontWeight="Bold" Margin="2,0,0,3"/>
                        <TextBox Text="{Binding NewLocation}" Width="300" Style="{StaticResource CyberInput}"/>
                    </StackPanel>

                    <Button Content="DEPLOY_NODE" Command="{Binding AddWarehouseCommand}" 
                            Style="{StaticResource CyberButton}" Margin="30,0,0,0" VerticalAlignment="Bottom" Width="180" Height="35"
                            BorderBrush="Lime" Foreground="Lime"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#0AFFFFFF" CornerRadius="15" Padding="10">
                <DataGrid ItemsSource="{Binding Warehouses}" AutoGenerateColumns="False" 
                          Background="Transparent" BorderThickness="0" RowBackground="Transparent" 
                          CanUserAddRows="False" HeadersVisibility="Column">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2000FFFF"/>
                            <Setter Property="Foreground" Value="#00FFFF"/>
                            <Setter Property="Padding" Value="15,10"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#00FFFF"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="NODE_ID" Binding="{Binding Id}" Width="100"/>
                        <DataGridTextColumn Header="ALIAS_NAME" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="PHYSICAL_LOCATION" Binding="{Binding Location}" Width="2*"/>

                        <DataGridTemplateColumn Header="OPERATIONS" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="TERMINATE" Command="{Binding DataContext.DeleteWarehouseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}" Style="{StaticResource CyberButton}" 
                                            Foreground="#FF4444" BorderBrush="#FF4444" FontSize="9" Height="25"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>